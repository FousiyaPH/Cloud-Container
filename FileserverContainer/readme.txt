This script only works inside windows 2016 machine
The script will create a windows server core container and will install File services
The User can create folders and share the folder using File services available inside container.